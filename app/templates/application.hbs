  <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      {{#link-to 'index' class="navbar-brand"}}<font color="#d72444">Ad Challenge</font>{{/link-to}}
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
       <!-- this is the logout button -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
      <ul class="nav navbar-nav">

      {{#if session.isAuthenticated}}
               <button {{action 'logout'}}>Logout</button>
             {{/if}}
             </ul>
       </div>

      <div class="nav-form">
	      <form >
	        <div class="form-horizontal form-group form-group row">
	     		<div class="col-md-6 col-md-offset-2 col-md-5 col-md-offset-2">
	       			{{input type="email" value=emailAddress class="form-control" placeholder="Please type your e-mail address." autofocus="autofocus"}}
	     		</div>
	     		<div class="col-sm-offset-0 col-sm-4 col-md-3">
	       			<button class="btn btn-primary  btn-block" {{action 'saveInvitation'}} disabled={{isDisabled}}>Subscribe</button>
	     		</div>
	   		</div>
				{{#if responseMessage}}
     				<div class="alert alert-success alert-dismissible">
     					<button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
     						{{responseMessage}}
     				</div>
   				{{/if}}
	       </form>
	    </div>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
{{log session.currentUser}}
  {{outlet}}
